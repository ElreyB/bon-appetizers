require 'rails_helper'

describe Menu do
  it { should belong_to :event }
#   it { should validate_uniqueness_of :antipasti}
#   it { should validate_uniqueness_of :pasta}
#   it { should validate_uniqueness_of :main_dish}
#   it { should validate_uniqueness_of :dessert}
#   it { should validate_uniqueness_of :wine}
# end
